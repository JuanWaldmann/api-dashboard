import express, {type Request, type Response} from 'express';
//import { getRepo, getUserRepos } from '../services/github.ts';

//const router = express.Router();

/*
router.get('/openweather', async (req: Request, res: Response) => {
}) */

